# Empirica Core Dependencies
# Minimal requirements for basic functionality

# Core Framework
pydantic>=2.0.0            # Data validation and settings
pydantic-settings>=2.0.0   # Settings management
pyyaml>=6.0.0              # YAML configuration parsing

# MCP Integration (optional but recommended for IDE usage)
mcp>=1.0.0                 # Model Context Protocol

# Modality Switcher (optional - for multi-AI routing)
anthropic>=0.39.0          # Anthropic SDK (for various adapters)
requests>=2.31.0           # HTTP requests for API adapters

# Advanced Features (optional)
aiofiles>=23.2.1           # Async file I/O for reflex logger
libtmux>=0.36.0            # Tmux integration for dashboard (optional)
tiktoken>=0.5.0            # Token counting (optional)

# Optional Component Dependencies
# networkx>=3.0            # For code_intelligence_analyzer (optional)

# Development Dependencies (install with: pip install -e ".[dev]")
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.1.0
# pytest-mock>=3.11.0
# dirty-equals>=0.7.0
# ruff>=0.1.0
rich>=13.0                 # REQUIRED: The TUI/Visuals won't work without this
google-generativeai>=0.5   # REQUIRED: For Gemini 3 / Pro access
typer>=0.9                 # REQUIRED: If your CLI uses Typer (highly likely)
